import React, { useEffect, useState } from 'react';
import axios from 'axios';

const usePostHook =()=> {
  // const [slug, setSlug] = useState();
  // cost [tagId, setTagId] = useState();

  useEffect(() => {
    
    return () => {
      
    }
  }, []);
  
  return (
    <div>usePostHook</div>
  )
}

export default usePostHook;
